const addSVG = require('./add-svg');
const attachLibrary = require('./attach-library');
const dump = require('./dump');
const mock = require('./mock');

module.exports = {
  addSVG,
  dump,
  attachLibrary,
  mock,
};
